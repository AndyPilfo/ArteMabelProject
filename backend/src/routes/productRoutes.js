import { Router } from 'express'; import * as c from '../controllers/productController.js'; import { requireAdmin, requireAuth } from '../middleware/auth.js'; import { createReview, productReviews } from '../controllers/reviewController.js';
const router = Router(); router.get('/', c.listProducts); router.get('/:id', c.getProduct); router.get('/:productId/reviews', productReviews); router.post('/:productId/reviews', requireAuth, createReview); router.post('/', requireAuth, requireAdmin, c.createProduct); router.put('/:id', requireAuth, requireAdmin, c.updateProduct); router.delete('/:id', requireAuth, requireAdmin, c.deleteProduct); export default router;
